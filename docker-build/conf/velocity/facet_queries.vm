#**
 *  Display facets based on specific facet queries
 *  e.g.: facets specified by &facet.query=
 *#

#set($field = $response.response.facet_counts.facet_queries)

#set($showField = false)
#if($field.size() > 0)
    #foreach ($facet in $field)
      #if ($facet.value > 0)
        #set($showField = true)
      #end
    #end
  #end      

#if($showField)

<h2 class="ui ribbon large label" #annTitle("Facets generated by adding &facet.query= to the request")>
  Query Facets
</h2>
#display_facet_query($field, "", "")

#end