#**
 *  Display facets based on field values
 *  e.g.: fields specified by &facet.field=
 *#

#if($response.facetFields)
  <h2 class="ui ribbon large label" #annTitle("Facets generated by adding &facet.field= to the request")>
    Field Facets
  </h2>
  <div class="ui basic segment">
  #set($facet_results_empty = true)
  #foreach($field in $response.facetFields)
    ## Hide facets without value
    #if($field.values.size() > 0)
      #set($facet_results_empty = false)
      <span class="facet-field ui tag label">$field.name</span>
      <ul>
        #foreach($facet in $field.values)
          <li><a href="#url_for_facet_filter($field.name, $facet.name)">#if($facet.name!=$null)$facet.name#else<em>missing</em>#end</a> ($facet.count)</li>
        #end
      </ul>
    #end  ## end if > 0
  #end    ## end for each facet field
#if ($facet_results_empty == true)
    No facets found.
#end
    </div>
#end      ## end if response has facet fields
